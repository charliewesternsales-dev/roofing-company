# Add a verified public origin to enable indexing and the sitemap.
NEXT_PUBLIC_SITE_URL=
# Server-only HTTPS webhook that receives validated estimate inquiries as JSON.
ESTIMATE_WEBHOOK_URL=
ESTIMATE_WEBHOOK_TOKEN=
